# 更新日志 Changelog

## 版本更迭规则 Rule of Versioning

本项目遵循 **语义化版本（SemVer）的简化版** `主版本.次版本.修订号`（`MAJOR.MINOR.PATCH`），每个里程碑为一个 `## Version X.Y.Z` 区块，**新的在上、旧的在下**，按分类记录：`新增功能` / `修改` / `修复`。

- **主版本号（Major）**：不兼容的重大改动。如架构重构、路由/接口整体变更、数据模型大规模破坏性调整、上线策略或对外契约的改变。
- **次版本号（Minor）**：新增向后兼容的功能。如新增页面、新接口、新数据表、新组件等正向能力扩展。
- **修订号（Patch）**：向后兼容的缺陷修复与小改进。如 Bug 修复、性能优化、文案/样式微调、安全性补丁。
- **0.y.z 阶段（正式发布前）**：次版本（Minor）可包含少量破坏性调整，不必严格遵守「Minor 必须兼容」；一旦发布 `1.0.0` 后即完全遵循上述规则。
- 何时递增：产生任一新增功能 → 递增 **次版本**；只有纯修复/微调 → 递增 **修订号**；存在不兼容重大改动 → 递增 **主版本**（同时可重设后两段）。
- 每一版变更建议同步更新项目内版本号来源（如需）与本文件记录，便于回归与发布留痕。

---

## Version 0.0.2

### 新增功能
- **访客统计（按 IP 去重）**：新增 `VisitLog` 表与 `POST /api/visits` 轻量接口，前端页面加载时上报一次，后端记录访问来源 IP 的哈希（隐私化存储，不保存明文）。
  - 管理页底部新增「访客统计」卡片：展示历史累计唯一访客数 + 近 7 天每日唯一访客趋势（简单柱状图，今日高亮）。
  - 口径：同一 IP 同一自然日只记一条（`ipHash + day` 唯一约束），天然去重；历史累计 = `COUNT(DISTINCT ipHash)`，某日唯一 = 该日行数；存储盐取 `VISIT_SALT` 环境变量，保证部署/重启后哈希稳定。
  - 接口：记录无鉴权但失败不影响页面（返回 204）；统计接口 `GET /api/admin/visits` 仅站长/管理员可见。
- **头像圆形裁剪**：新增通用图片裁剪组件 `ImageCropper.vue`（基于 cropperjs `^1.6.2`，依赖已加入 `client/package.json`），支持圆形/方形裁剪框、拖拽与滚轮缩放、可配置输出尺寸。
  - 个人资料编辑页：选择头像图片后弹出裁剪面板，可调整圆框位置与大小，确认后上传裁剪结果，替代原先直接上传整图。
  - 组件通过 props/事件对外解耦（`open`/`src`/`shape`/`aspectRatio`/`outputSize` + `confirm` 回调返回裁剪 Blob），上传职责由调用方承担，便于库封面等场景后续复用。
- **评测卡片整卡可点击**：评测库与游戏详情页的评测卡片，点击卡片任意空白/正文区域即进入对应评测详情；命中自带跳转或交互的元素（链接、按钮、游戏名行）时不拦截，由它们各自处理。
- **评测卡片悬停反馈**：鼠标悬停时卡片边框变主色、加深阴影并提亮背景，提供 active 视觉反馈。
- **前端上传图片压缩**：新增 `imageCompress.js`，所有用户手动上传（头像、评测封面、库封面裁剪）统一经 `uploadImage` 先做「等比限宽 + 转 WebP」再上传，显著减小上传与存储体积。GIF 与小于 200KB 的图片跳过压缩；仅当压缩后更小才采用，压缩失败或更大则回退原图，保证不丢图。`ImageCropper` 默认输出 WebP（保留透明）。

### 修改
- **SGDB 封面下载自动压缩**：`downloadCover` 下载 SGDB 大图后经 sharp「等比限长边 1600 + 转 WebP(质量 82)」落盘，仅当压缩产物更小时采用，压缩失败或不更小则回退存原图。依赖新增 `sharp`，实测单张封面可省约 90% 体积，避免新拉取的图以原尺寸累积。
- **本地存量图一次性压缩**：用临时脚本将 `server/uploads` 存量 PNG/JPG 批量转 WebP（约 346MB→53MB，-84.7%），并同步更新数据库与数据 JSON 中的图片 URL 引用；原图完整备份于 `server/uploads/backup-originals/` 以便回退。（说明：此为一次性本地数据迁移，非代码改动；远程服务器存量图仍为原尺寸，需另行处理。）
- **资料编辑后整页刷新**：保存个人资料后改为整页跳转主页，强制导航栏、个人主页等各处重新加载最新昵称/头像，避免 SPA 局部跳转沿用旧缓存。
- **去掉 “Gamer” 用户名前缀**：未填写用户名（nickname）时统一默认显示账号名，不再拼接 `Gamer` 前缀；同步更新注册页占位提示与个人主页/评测卡片的显示逻辑。

### 修复
- **分项评分文字内容不再必填**：分项评分仅需「方向 + 0-10 整数分」即可提交，去掉了「文字描述」的必填校验（正文逻辑保持不变）。
- **站长删除他人评测改为软删**：仅作者本人删除自己的评测时才硬删除；站长/管理员删除他人评测一律进入不可见列表（软删），不再直接硬删。
- **评测排名兼容 MySQL 5.7**：移除 `ROW_NUMBER() OVER` 窗口函数（5.7 不支持），改为普通查询并在 JS 内分组计算排名，本地（5.7）与服务器（8.0）均可运行，修复评测页/个人主页排行榜卡加载问题。

---

## Version 0.0.1

初始版本基线，并部署至线上 `gamerank.garfieldgod.cn`。

### 功能
- **游戏库**：游戏新增/编辑/删除、库封面与详情页、标签筛选、关键词搜索、综合评分与排行榜。
- **评测系统**：撰写/查看评测、分项评分、点赞/不认可、评测库列表与个人排名。
- **游戏申请**：普通用户提交「新增/编辑」申请，站长/管理员审批后生效。
- **用户系统**：登录/注册/个人主页、资料编辑（昵称/头像/简介）、基于用户名的访问路由。
- **管理后台**：用户管理、软删除与不可见列表、数据导入/导出。
- **体验与部署**：中英文切换、深色主题、图片懒加载与渐变淡入；Nginx + HTTPS 部署、导出/导入/备份脚本。